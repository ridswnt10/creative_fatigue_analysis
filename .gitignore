# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Jupyter Notebook
.ipynb_checkpoints

# Raw data files (large - must be downloaded separately)
data/raw/*.gz
data/raw/*.csv
data/raw/*.tsv
!data/raw/.gitkeep
!data/raw/README.md

# Processed and sample data are INCLUDED for reproducibility
# (uncomment below to exclude if files become too large)
# data/processed/*.csv
# data/samples/*.csv
# data/samples/*.json

# Model artifacts (large files)
results/models/*.pkl
results/models/*.joblib
!results/models/.gitkeep

# IDE
.vscode/
.idea/
*.swp
*.swo

# PDFs in pdfs/ folder (local generated, can be regenerated)
pdfs/
# Note: notebook_pdfs/ is included for easy viewing on GitHub

# OS
.DS_Store
Thumbs.db

